Granuality -> when obejefct has more classes than there are tables on datanbase , then we say obeject are granular thean the relational model

Hibernate provides some strategies 

1. Single Table Strategy -> all classses in the heirarchy area mapped to sigle table.
                            discriminator column is used to differentiate bw row beongin to different subclass
                            PROBLEM -> this strategy can lead to large table with many nullable columns especially if subclasses have diffrent attrobutes
2. Joined Table Stratrgy ->  Each class in the heirarchy has its own table
                            Problem-> this strtegy can lead to complex queries involving multiple joins, pottenttially affecting performance
3. Table per class strategy -> Each concerte class in the heirarchy has itas own table with all attributes from the class and superclass
                                Problems -> This stategy can leads to data redundancy
-------ends here -------
Incorrect Mapping ->  mistakes inb annotations can lead to incorrect table structres and data mappings.
                      Ffor example-> not specifying the correct discriminator column cause issues when retriving
Performance issues -> 

Query Complexity -> Complex inheritece heirarchies can make writing HQL or JPQL more difficult especially fdealing with polymorphic rellationship

Schema Evolution -> 

Discriminator issus -> 

Redundancy -> 

Complex Queries -> 

---------------------
Solutions and Best Practices

Careful Strategy Selection -> choose inheritence strategy that best fits the data model and querry patterns.

Proper Annotation Usage -> Double check the annotation for correctnress, especially @inheritence, @discriminatorCOlumn and @DiscriminatorValue

Use of @MappedSupperclass for shared attributes without separate table,
use @mappedSuperclass instead of @Inheritance

Optimized Queries -> use HQL or JPQL carefully

Filter Stratefoes -> implement custome filters to handle plymorphic queries

Schema Management -> Plan database schema evolutionslly carefully when making changes to inheritance hierrachy

Testing -> tsett he applicaton with different snarios to ensure correct mapping and data retreivals

Consider Alternatives -> if inheritancce heirrarchy is complex consider alternatives approaches like compositon or interfaces.

Common Annotations and usage
















-----------------
Two Types of caching used in Hibernate
1. First level cache
2. Second level cache

